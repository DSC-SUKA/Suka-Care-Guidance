<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_tunanetra_allbooks_extend"
    android:orientation="vertical"
    android:baselineAligned="false"
    tools:context=".fragment.ListeningBookFragment">

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <include
            android:id="@+id/tool_bar"
            layout="@layout/toolbar"/>

    </android.support.design.widget.AppBarLayout>

    <ImageButton
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:layout_gravity="center"
        android:layout_marginTop="100dp"
        android:background="@android:color/transparent"
        android:contentDescription="@string/dengarkan_buku"
        android:fitsSystemWindows="false"
        android:scaleType="fitCenter"
        android:src="@drawable/btn_audio" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="horizontal"
        android:gravity="bottom|center"
        android:layout_weight="1"
        android:layout_margin="@dimen/space">

        <Button
            android:layout_width="125dp"
            android:layout_height="wrap_content"
            android:text="@string/repeat"
            android:textAllCaps="false"
            android:background="@drawable/transparent_round_button"
            tools:ignore="ButtonStyle"
            android:layout_marginRight="50dp"/>

        <Button
            android:layout_width="125dp"
            android:layout_height="wrap_content"
            android:textAllCaps="false"
            android:text="@string/add_to_my_book"
            android:background="@drawable/blue_round_button"
            android:textColor="@android:color/white"
            tools:ignore="ButtonStyle"/>

    </LinearLayout>

</LinearLayout>
